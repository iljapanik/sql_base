SELECT
	NAME
FROM
	PIZZERIA
WHERE
	NOT EXISTS (
		SELECT
			PIZZERIA_ID
		FROM
			PERSON_VISITS
		WHERE
			PIZZERIA_ID = PIZZERIA.ID
	);

SELECT
	NAME
FROM
	PIZZERIA
WHERE
	ID NOT IN (
		SELECT
			PIZZERIA_ID
		FROM
			PERSON_VISITS
	);