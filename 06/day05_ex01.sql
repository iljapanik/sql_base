SET
	ENABLE_SEQSCAN TO ON;

EXPLAIN
ANALYZE
SELECT
	MENU.PIZZA_NAME,
	PIZZERIA.NAME
FROM
	MENU
	JOIN PIZZERIA ON MENU.PIZZERIA_ID = PIZZERIA.ID;

SET
	ENABLE_SEQSCAN TO OFF;

EXPLAIN
ANALYZE
SELECT
	MENU.PIZZA_NAME,
	PIZZERIA.NAME
FROM
	MENU
	JOIN PIZZERIA ON MENU.PIZZERIA_ID = PIZZERIA.ID;